model:
  n_estimators: 200
  learning_rate: 0.05
  max_depth: 6
  num_leaves: 31

balancing:
  k_neighbors: 3         # SMOTE-NC (ver ADR-003) -- único método usado

features:
  temporais: true       # true | false -- inclui dia_de_semana e horario
