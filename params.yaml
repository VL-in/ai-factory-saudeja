model:
  n_estimators: 200
  learning_rate: 0.05
  max_depth: 6
  num_leaves: 31

balancing:
  strategy: smotenc      # none | class_weight | smotenc
  k_neighbors: 3        # só usado quando strategy == smotenc
