# Sobre o model.pkl

Este arquivo deveria existir aqui mas não foi versionado por ser binário.
O aluno deve gerá-lo executando o `notebook.ipynb` ou `python train.py`.

Se o aluno quiser pular o treino e ir direto para deploy/API:
- Execute o notebook 1x (treino leva ~30s)
- O arquivo `model.pkl` aparece na raiz do projeto
